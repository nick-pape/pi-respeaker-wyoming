# Wyoming Satellite Configuration
# Edit these values before running install.sh

# Name shown in Home Assistant (use quotes for spaces)
SATELLITE_NAME="living-room-satellite"

# Wyoming satellite listen port
SATELLITE_PORT=10700

# Audio device (auto-detected after driver install, usually this is correct)
AUDIO_DEVICE="plughw:CARD=seeed2micvoicec,DEV=0"

# Mic settings
MIC_VOLUME_MULTIPLIER=1.0
MIC_AUTO_GAIN=5
MIC_NOISE_SUPPRESSION=2

# Wake word mode: "remote" or "local"
# remote = HA server runs openWakeWord (RECOMMENDED for Pi Zero 2 W)
# local  = this device runs openWakeWord (risky on Pi Zero 2 W - CPU/stability)
WAKE_WORD_MODE="remote"

# Wake word name (only used if WAKE_WORD_MODE=local)
# Built-in options: ok_nabu, hey_jarvis, alexa, hey_mycroft, hey_rhasspy
# Custom: place .tflite in ~/custom-wake-words/ and use the filename without extension
WAKE_WORD_NAME="ok_nabu"

# Enable ReSpeaker LED feedback (lights up on wake/processing/done)
ENABLE_LEDS=true
